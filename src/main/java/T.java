import java.util.regex.Matcher;
import java.util.regex.Pattern;
import com.pythian.Patient;

public class T {
  public static void main(String[] argv) {
     Patient r = new Patient("ablue", "", "", "");
     System.out.println("Id: " + r.getId());
  }
}
